services:
  - type: web
    name: omni-retail-orchestrator
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python server.py
    envVars:
      - key: OPENROUTER_API_KEY
        sync: false  # Set this in Render dashboard
      - key: PORT
        value: 10000  # Render's default port
